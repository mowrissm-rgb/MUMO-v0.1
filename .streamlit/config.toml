# No forced theme colors — MUMO follows the user's theme choice.
# Users can switch Light / Dark / "Use system setting" from the app's
# top-right menu → Settings → Theme. Text colour adapts automatically
# (dark theme → light text, light theme → dark text).

[server]
headless = true
port = 8501

[browser]
gatherUsageStats = false
